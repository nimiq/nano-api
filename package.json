{
  "name": "@nimiq/nano-api",
  "version": "0.1.0-beta.1",
  "description": "Nano Network API",
  "main": "dist/NanoApi.umd.js",
  "module": "dist/NanoApi.es.js",
  "types": "types/index.d.ts",
  "repository": "git@github.com:nimiq/nano-api.git",
  "author": "Nimiq",
  "license": "Apache-2.0",
  "scripts": {
    "build": "rm -rf build && rm -rf dist && tsc && rollup -c"
  },
  "files": [
    "dist",
    "types/index.d.ts"
  ],
  "dependencies": {
    "@nimiq/utils": "^0.2.0"
  },
  "devDependencies": {
    "@nimiq/core-types": "^1.4.2",
    "rollup": "^0.65.0",
    "typescript": "^3.3.3333"
  }
}
